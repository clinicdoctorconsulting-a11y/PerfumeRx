[build]
  command   = "npm run build"
  publish   = "dist"

# Redirect all routes to index.html so React Router (or future routing) works
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
